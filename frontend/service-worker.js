// Cache shell assets on install (index.html, map.js, ui.js, style.css).

// On fetch: serve cached for shell assets, network-first for API calls.

// Queue failed POST /rating to IndexedDB when offline.
// Replay queue on next sync event (Background Sync API).